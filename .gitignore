# Environments and secrets
.env
.venv/
venv/
*.pem
*.json.key
credentials.json
service-account*.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Generated artefacts (reproducible via `fcca generate-data` / `fcca ingest-policies`)
data/raw/
data/processed/
data/evaluation/
audit.db
*.jsonl.tmp

# Audit exports may contain full case detail; benchmark results are committed on
# purpose so the comparison table travels with the repository.
results/*.jsonl

# OS
.DS_Store

# macOS/iCloud sync duplicates ("file 2.py"). Untracked, but a `git add -A`
# would publish them; the pattern is never a legitimate filename here.
* 2.*
* 2
* [0-9].py

# Local tooling config.
.claude/
